import console from "console";

global.console = console;

describe("Withdrawals endpoints ", () => {
  it(
    "can navigate throught withdrawals headers" + "(GET:  /withdrawals)",
    async () => {
      // TODO
    }
  );
});
